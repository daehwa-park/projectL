using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using UnityEngine.SceneManagement;
using System.IO;

public class SettingPannelManager : MonoBehaviour
{
    // Start is called before the first frame update
    /*
    public Button SetPannel_Off_Btn;

    void Start()
    {
        //setting_Pannel = WorldToMoveManager.instance.SetPannel;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    public void Btn_onclick()
    {
        SetPannel_Off_Btn.onClick.AddListener(() => setExit_Btn_onclick());
    }
    public void setExit_Btn_onclick()
    {
        WorldToMoveManager.instance.SetPannel.SetActive(false);
        //setting_Pannel.SetActive(false);
        WorldToMoveManager.instance.SetPannel_On_Btn.gameObject.SetActive(true);

    }*/ 
}
